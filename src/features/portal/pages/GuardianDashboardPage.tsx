import { CalendarDays, CreditCard, UsersRound } from 'lucide-react';
import type { ReactNode } from 'react';
import { useEffect, useState } from 'react';

import { useAuth } from '../../auth/AuthProvider';
import { fetchGuardianOverview, type GuardianOverview } from '../guardian.api';

export function GuardianDashboardPage() {
  const { accessToken, user } = useAuth();
  const [overview, setOverview] = useState<GuardianOverview>();
  const [errorMessage, setErrorMessage] = useState<string>();
  useEffect(() => { if (accessToken) void fetchGuardianOverview(accessToken).then(setOverview).catch((error: unknown) => setErrorMessage(error instanceof Error ? error.message : 'Não foi possível carregar suas informações.')); }, [accessToken]);
  const attendanceRate = overview?.attendance.total ? Math.round((overview.attendance.present / overview.attendance.total) * 100) : undefined;

  return <main className="px-6 py-20 lg:px-10 lg:py-10"><p className="text-xs font-bold tracking-[.18em] text-mezzo-purple">PORTAL DO RESPONSÁVEL</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-.04em]">Olá, {user?.name || user?.email}.</h1><p className="mt-3 text-stone-600">Acompanhe agenda, presença e financeiro dos alunos vinculados à sua conta.</p>{errorMessage && <p className="mt-6 rounded-md bg-red-50 p-4 text-sm font-medium text-red-800" role="alert">{errorMessage}</p>}<section className="mt-10 grid gap-4 md:grid-cols-3"><Metric icon={<UsersRound />} label="Alunos vinculados" value={overview?.students.length.toLocaleString() ?? '—'} /><Metric icon={<CalendarDays />} label="Próximas aulas" value={overview?.upcomingLessons.length.toLocaleString() ?? '—'} /><Metric icon={<CreditCard />} label="Faturas em aberto" value={overview?.invoices.length.toLocaleString() ?? '—'} /></section><section className="mt-8 grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><article className="rounded-2xl bg-white p-7 shadow-sm"><p className="text-xs font-bold tracking-[.16em] text-mezzo-purple">AGENDA</p><h2 className="mt-2 font-display text-3xl font-bold">Próximas aulas</h2>{!overview && <p className="mt-7 text-sm text-stone-600">Carregando agenda...</p>}{overview?.upcomingLessons.length === 0 && <Empty text="Não há aulas futuras para os alunos vinculados." />}{overview && overview.upcomingLessons.length > 0 && <ul className="mt-6 divide-y divide-mezzo-purple/10">{overview.upcomingLessons.map((lesson) => <li className="flex flex-wrap items-center justify-between gap-4 py-4" key={lesson.id}><div><p className="font-bold">{lesson.course}</p><p className="mt-1 text-sm text-stone-600">{lesson.student} · {lesson.teacher}</p></div><time className="text-right text-sm font-bold text-mezzo-purple" dateTime={lesson.startsAt}>{formatDate(lesson.startsAt)}<br />{formatTime(lesson.startsAt)}</time></li>)}</ul>}</article><article className="rounded-2xl bg-white p-7 shadow-sm"><p className="text-xs font-bold tracking-[.16em] text-mezzo-purple">ACOMPANHAMENTO</p><h2 className="mt-2 font-display text-3xl font-bold">Frequência</h2><p className="mt-7 font-display text-5xl font-bold text-mezzo-purple">{attendanceRate === undefined ? '—' : `${attendanceRate}%`}</p><p className="mt-2 text-sm text-stone-600">{overview ? `${overview.attendance.present} presença(s) e ${overview.attendance.absent} ausência(s) registradas.` : 'Carregando frequência...'}</p><h3 className="mt-8 font-display text-2xl font-bold">Financeiro</h3>{overview?.invoices.length === 0 && <Empty text="Não há faturas pendentes no momento." />}{overview && overview.invoices.length > 0 && <ul className="mt-4 divide-y divide-mezzo-purple/10">{overview.invoices.map((invoice) => <li className="py-3 text-sm" key={invoice.id}><p className="font-bold">{invoice.student} · {invoice.description}</p><p className="mt-1 text-stone-600">Vencimento: {formatDate(invoice.dueDate)} · <strong className="text-mezzo-purple">{formatCurrency(invoice.amountInCents)}</strong></p></li>)}</ul>}</article></section></main>;
}

function Metric({ icon, label, value }: { icon: ReactNode; label: string; value: string }) { return <article className="rounded-2xl bg-white p-6 shadow-sm"><div className="flex items-start justify-between"><p className="text-sm font-bold text-stone-600">{label}</p><span className="text-mezzo-purple">{icon}</span></div><p className="mt-7 font-display text-3xl font-bold">{value}</p></article>; }
function Empty({ text }: { text: string }) { return <p className="mt-6 rounded-xl border border-dashed border-mezzo-purple/25 p-5 text-center text-sm text-stone-600">{text}</p>; }
function formatCurrency(cents: number) { return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(cents / 100); }
function formatDate(value: string) { return new Intl.DateTimeFormat('pt-BR', { day: '2-digit', month: 'short' }).format(new Date(value)); }
function formatTime(value: string) { return new Intl.DateTimeFormat('pt-BR', { hour: '2-digit', minute: '2-digit' }).format(new Date(value)); }
